import React from 'react';
import Layout from '../components/layout';

export default () => (
  <Layout title="Page not found" keywords={[`404`, `page not found`]}>
    404 - Page not found :(
  </Layout>
);
